# 构建工具（本地开发用，不上传服务器）
build.ps1

# 源码（仅本地保留，仓库只放压缩后的 scripts.min.js）
scripts.js

# 临时/系统文件
.trae/
_check_lines.ps1
_extract.ps1

# 自测脚本（本地验证用，不上传仓库）
_test_*.js

# 性能测试脚本与报告（本地使用，不上传仓库）
perf-test.js
performance-report.md

# 测试依赖（本地 puppeteer-core，不上传）
node_modules/
package.json
package-lock.json
